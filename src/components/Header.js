import NavBar from "./NavBar";

function Header() {
  return (
    <header>
      <div style={{ padding: '10px' }}>
        {/* Nav component */}
        <NavBar />
      </div>
    </header>
  );
}

export default Header;
